# Brand documents

The originals were not in this repository when the marketing site was built.

| File | Status |
| --- | --- |
| `REPLYLANE_BRAND_IDENTITY.md` | Missing — tokens, type, lamp, and icon technique taken from the build prompt; Appendix B paths regenerated |
| `REPLYLANE_SITE_KIT.md` | Missing — voice rules and hero structure taken from the build prompt |
| `REPLYLANE_LANDING_PROMPT.md` | Missing — unused |

Drop the real files here to replace the synthesized notes below.
